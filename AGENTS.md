# AGENTS.md

## 基本方針

* 変更は必要最小限にしてください。
* 関係のないリファクタリングや仕様変更を行わないでください。
* リポジトリ全体を最初から無差別に読み込まないでください。
* まずファイル名検索、文字列検索、参照元検索を使用して対象を絞り込んでください。
* `node_modules`、`.next`、`dist`、`build`、`coverage`、自動生成ファイルは調査対象外としてください。

## コメント・JSDoc

* 新しい関数、コンポーネント、Server Action、API、共通処理を追加する場合は、コードだけでは役割や前提条件を理解しにくいときに、関数上部へJSDocを記載してください。
* 既存処理を改修する場合も、変更によって処理の目的、前提条件、副作用、例外条件、依存関係が分かりにくくなる場合は、JSDocまたは処理付近のコメントを追加・更新してください。
* すべての関数へ機械的にJSDocを追加しないでください。関数名、型定義、短い実装だけで役割が十分に理解できる場合は不要です。
* コメントでは、コードを日本語で言い換えるのではなく、コードから読み取りにくい「なぜこの処理が必要なのか」「どのような制約があるのか」を優先して説明してください。
* 特に次の処理では、必要に応じてJSDocまたはコメントを記載してください。
  * アプリ固有の業務ルールを扱う処理
  * 日付、時刻、JST、UTC、お世話日の境界を扱う処理
  * 認証、認可、権限判定を扱う処理
  * DBトランザクションや複数テーブルの更新を行う処理
  * リアルタイム同期、revision、SSE、polling、通知を扱う処理
  * 副作用、例外、フォールバックを伴う処理
  * 呼び出し順序や利用条件に制約がある処理
  * 引数や戻り値に特殊な意味がある処理
* 関数全体の役割、前提条件、引数、戻り値、副作用、例外条件は、原則として関数上部のJSDocへ記載してください。
* 特定の分岐、計算式、例外処理、回避策についての説明は、その処理付近の通常コメントとして記載してください。
* TypeScriptの型情報から明らかな内容を、JSDocで重複して説明しないでください。
* `@param`、`@returns`、`@throws`は、型や関数名だけでは意味が分からない場合にのみ使用してください。
* `pad`のような単純な補助関数、名前どおりのgetter、単純なstate更新、短いイベントハンドラーには、原則としてJSDocを追加しないでください。
* 既存コメントと実装が一致しなくなった場合は、実装変更と同時にコメントも更新してください。
* JSDocと通常コメントに同じ説明を重複して記載しないでください。
* 実装や参照元から確認できない仕様を推測してコメントへ記載しないでください。
* コメントだけでは処理を正確に説明できないほど実装が複雑な場合は、コメントを増やすだけで対応せず、必要に応じてリファクタリング候補として報告してください。ただし、依頼範囲外のリファクタリングは行わないでください。

## 日付・時刻

* DB の timestamp は UTC で保存・比較し、利用者向け画面へ描画するときは必ず JST（日本標準時）へ変換してください。
* `createdAt`、`updatedAt`、`expiresAt`、`acceptedAt`、`revokedAt` など時刻を持つ値の表示には、`src/lib/date.ts` の JST 表示用関数を使用してください。
* 測定日、掃除日、誕生日など時刻を持たない日付はタイムゾーン変換せず、DB に保存された暦日を維持してください。CSV の明示的な UTC 出力や内部ログは画面表示とは別仕様として扱ってください。

## 機能マップ

* 機能と関連ファイルの対応は `docs/feature-map.md` に記載されています。
* 修正対象を探す際は、最初に `docs/feature-map.md` の該当機能を確認してください。
* 機能マップに記載されたファイルと、その直接の依存先から調査を開始してください。
* 情報が不足している場合だけ、検索を使用して調査範囲を広げてください。
* 実装によって関連ファイルや依存関係が変わった場合は、`docs/feature-map.md` の該当箇所だけ更新してください。
* 機能マップの更新を理由に、無関係な機能を再調査しないでください。

## README.md の保護

`README.md` はリポジトリ所有者が手動で管理するファイルです。

Codexは、通常の実装・修正・リファクタリング・ドキュメント更新作業において、`README.md` を変更してはいけません。

以下の操作を禁止します。

* `README.md` の内容変更
* 追記、削除、校正、整形、フォーマット変更
* 見出し、リンク、バッジ、画像、コード例の追加・変更・削除
* `README.md` の削除、移動、リネーム、再生成
* 他のドキュメントからの内容同期
* 実装内容に合わせた自動更新
* 「ドキュメントを更新してください」という一般的な指示を、`README.md` の編集許可として解釈すること

開発者向け、運用者向け、デプロイ向けの情報を更新する必要がある場合は、`README.md` ではなく `docs/` 配下の適切なファイルを更新してください。

実装変更によって `README.md` の内容が古くなった場合も、Codex自身では修正しないでください。作業結果の最後に、必要と思われるREADME変更内容を提案として報告するだけにしてください。

`README.md` の編集が必要に見える場合は、作業を進める前に明示的に報告してください。ただし、リポジトリ所有者から現在の依頼内で `README.md` の編集を明確に許可されていない限り、変更してはいけません。

作業完了前に、次のコマンドで `README.md` に差分がないことを確認してください。

```bash
git diff -- README.md
```

差分が存在する場合は、勝手に `git restore`、`git checkout`、上書き保存を行わないでください。既存の利用者による未コミット変更を消す可能性があるため、差分が存在することを報告して作業を停止してください。

## 検証

* 今回の変更に直接関係するテスト、型チェック、Lintを優先してください。
* 全体テストや全体ビルドは、変更内容から必要と判断できる場合のみ実行してください。

## 完了報告

以下だけを簡潔に報告してください。

* 変更したファイル
* 変更内容
* 実行した確認
* 機能マップの更新有無
* 残っている問題
